#ifndef UTILS_H
#define UTILS_H

/**
 * @brief This class is for utility functions which will be used generally by the 
 * main function for certain repeated tasks.
 */
class Utility{

    public:
        /**
         * @brief this function clears the terminal(screen) every any time it is called
         */
        void clearScreen();

};

 #endif //UTILS_H